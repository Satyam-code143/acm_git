import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
  titleContainer: {
    width: "100%",
  },
  pageTitleStyle: {
    textDecoration: "underline",
    color: "#004488",
    textTransform: "upperCase",
    fontWeight: "bold",
    textDecorationColor: "#693F23",
    margin: 30,
  },
}));
